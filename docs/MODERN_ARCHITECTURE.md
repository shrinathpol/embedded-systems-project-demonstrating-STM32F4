# 📐 Modern Project Architecture

## System Architecture Layers

```
┌─────────────────────────────────────────────────────────────┐
│                    Application Layer                        │
│                      (main.c)                               │
│  Handles main logic, initialization, control flow           │
└────────────────────────┬────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                  Middleware Layer                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │   Command    │  │    Logger    │  │ Calibration  │       │
│  │   Parser     │  │  (Flash)     │  │  Manager     │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
│                                                             │
│  High-level features and user-facing functionality          │
└────────────────────────┬────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                   Drivers Layer                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │    Ring      │  │     CRC      │  │   Sensors    │       │
│  │   Buffer     │  │  Checksum    │  │  Utilities   │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
│                                                             │
│  Reusable utility modules                                   │
└────────────────────────┬────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                  Core HAL Layer                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │     ADC      │  │     DMA      │  │    Timer     │       │
│  │   Driver     │  │   Driver     │  │   Driver     │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │    UART      │  │    Error     │  │   Watchdog   │       │
│  │   Driver     │  │   Handler    │  │    Timer     │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
│                                                             │
│  Hardware abstraction and initialization                    │
└────────────────────────┬────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                 Hardware Layer                              │
│           STM32F411CE Microcontroller                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │     ADC1     │  │   DMA2       │  │    TIM2      │       │
│  │              │  │   Stream 0   │  │              │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │   USART1     │  │    GPIO      │  │    NVIC      │       │
│  │   115200     │  │   PA0-PA9    │  │  Interrupt   │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
└─────────────────────────────────────────────────────────────┘
```

## Module Dependency Map

```
main.c
  ├─────► core/uart.c       ──► Include: uart.h
  │
  ├─────► core/adc.c        ──► Include: adc.h
  │       └────► utils/error.c
  │
  ├─────► core/dma.c        ──► Include: dma.h
  │
  ├─────► core/timer.c      ──► Include: timer.h
  │
  ├─────► drivers/buffer.c  ──► Include: buffer.h
  │
  ├─────► middleware/command.c  ──► Include: command.h
  │       └────► drivers/crc.c
  │
  ├─────► utils/error.c     ──► Include: error.h
  │
  └─────► include/config.h  ──► All modules depend on this
```

## Data Flow in Real-Time

```
Time → 

0ms   ┌─ Timer fires (100 Hz, every 10ms)
      │  ├─ Sends TRGO trigger to ADC
      │  └─ ADC starts conversion
      │
1ms   │  ├─ ADC completes 12-bit conversion
      │  └─ DMA moves value to RAM
      │
2ms   │  ├─ DMA transfer complete
      │  └─ ISR sets completion flag
      │
3ms   ├─ Main loop detects flag
      │  ├─ Calculates voltage: (raw * 3300) / 4095
      │  ├─ Formats UART message
      │  └─ Transmits: "ADC: 2048 | Volts: 1.648 V"
      │
10ms  ├─ Timer fires again (cycle repeats)
      │
      ▼
```

## State Machine

```
┌──────────────┐
│  POWERUP     │
└──────┬───────┘
       │
       ▼
┌──────────────────────────┐
│   INITIALIZATION         │
│  └─ Setup GPIO           │
│  └─ Setup Clock          │
│  └─ Setup ADC            │
│  └─ Setup DMA            │
│  └─ Setup Timer          │
│  └─ Setup UART           │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│   ARMED                  │
│  └─ Timers running       │
│  └─ ADC sampling         │
│  └─ DMA transferring     │
└──────┬───────────────────┘
       │
    ┌──┴──┐
    │     └─────────────────┐
    ▼                       ▼
┌──────────────┐   ┌──────────────┐
│  PROCESSING  │   │    ERROR     │
│ Convert data │   │ Handle fault │
│ Send UART    │   │ Log error    │
└──────┬───────┘   └──────┬───────┘
    │                     │
    └──────────┬──────────┘
               │
               ▼
         (Loop back)
```

## Memory Management

```
STM32F411CE Memory Map
┌─────────────────────────────────────┐  0x00000000
│  Flash Memory (512 KB)              │
│  ├─ Vector Table (0x200)            │
│  ├─ Code & Constants (~20 KB)       │
│  ├─ Read-Only Data                  │
│  └─ Available (~472 KB)             │
├─────────────────────────────────────┤  0x20000000
│  SRAM (128 KB)                      │
│  ├─ .bss (uninitialized data)       │
│  ├─ .data (initialized data)        │
│  ├─ Buffers (ADC, UART, Log) ~6KB   │
│  ├─ Stack (grows down) ~2KB         │
│  └─ Heap (grows up) ~1KB            │
├─────────────────────────────────────┤
│  Peripheral Registers               │
│  └─ GPIO, ADC, DMA, UART, etc.      │
└─────────────────────────────────────┘
```

## Compilation Process

```
┌─────────────────────┐
│  Source Files       │
│  ├─ src/core/*.c    │
│  ├─ src/drivers/*.c │
│  ├─ src/utils/*.c   │
│  └─ src/main.c      │
└──────────┬──────────┘
           │
           ▼ (Compile)
┌─────────────────────┐
│  Object Files       │
│  ├─ core/*.o        │
│  ├─ drivers/*.o     │
│  └─ utils/*.o       │
└──────────┬──────────┘
           │
           ▼ (Link with STM32 HAL)
┌─────────────────────┐
│  Executable         │
│  └─ firmware.elf    │
└──────────┬──────────┘
           │
           ▼ (Convert format)
┌─────────────────────┐
│  Binary Image       │
│  └─ firmware.bin    │
└──────────┬──────────┘
           │
           ▼ (Upload)
┌─────────────────────┐
│  STM32 Flash        │
│  └─ Running!        │
└─────────────────────┘
```

## Interrupt Priority Table

| Priority | IRQ | Source | Module | Purpose |
|----------|-----|--------|--------|---------|
| 0 | DMA2_Stream0 | ADC data ready | core/adc.c | ADC→RAM transfer complete |
| 1 | TIM2 | Timer overflow | core/timer.c | Sampling trigger (optional) |
| 2 | USART1 | RX/TX event | core/uart.c | Serial communication |
| High | SysTick | System tick | core/system.c | 1ms tick (future) |

## Configuration Propagation

```
include/config.h
  │
  ├─► include/core/adc.h
  │   └─► src/core/adc.c
  │
  ├─► include/core/timer.h
  │   └─► src/core/timer.c
  │
  ├─► include/drivers/buffer.h
  │   └─► src/drivers/buffer.c
  │
  ├─► include/utils/error.h
  │   └─► src/utils/error.c
  │
  └─► src/main.c

All configurable parameters in one place!
```

---

*Last Updated: January 14, 2026*
*Architecture Version: 2.0 (Modular)*
